void main() {
  /*  // stdout.write("Enter your name:");
  // String? userName = stdin.readLineSync();
  // stdout.write("Enter your age:");
  // String? userAge = stdin.readLineSync();
  // print("my name is $userName and i am $userAge years old");

  stdout.write("Enter first number");
  String? x = stdin
      .readLineSync()!; // ? = nullable, ! = null check for the operation
  int xInt = int.parse(x);

  stdout.write("Enter second number");
  String? y = stdin.readLineSync()!;
  int yInt = int.parse(y);

  print(xInt + yInt);

  stdout.write("Enter your height:");
  String? z = stdin.readLineSync();
  double zDouble = double.parse(z!);
  print(zDouble);

  stdout.write("Enter your weight:");
  String? weight = stdin.readLineSync();
  double weightDou = double.parse(weight!);
  print("BMI is ${weightDou + zDouble}"); */
  /* 
  stdout.write("Enter first number:");
  dynamic num1 = stdin.readLineSync()!;
  stdout.write("Enter Second number:");
  dynamic num2 = stdin.readLineSync()!;

  print(num1);
  print(num2);
  print(num1 / num2); // Error during runtime  */

  /* stdout.write("Enter first number:");
  var num1 = int.parse(stdin.readLineSync()!);
  stdout.write("Enter Second number:");
  var num2 = int.parse(stdin.readLineSync()!);

  print(num1);
  print(num2);
  print(num1 / num2); // Error during runtime
 */
}
